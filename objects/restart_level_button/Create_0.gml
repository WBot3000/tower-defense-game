/// @description This button isn't being used because there's a glitch with the menu UIs where the buttons don't display after the room restarts. Initialize variables
event_inherited();
highlighted = false;