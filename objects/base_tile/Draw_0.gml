/// @description For drawing a highlight
// You can write your code in this editor
if(highlight != noone) {
	draw_sprite(highlight, 1, x, y); //TODO: Try fixing this error. Doesn't actually cause issues during execution but it's bugging me
}