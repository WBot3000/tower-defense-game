/// @description Declare some constants
/*
show_menu = false;

menu_width = window_get_width()/3;
menu_height = window_get_height();

var _button_width = object_get_sprite(unit_purchase_button).width;

x_gap = (menu_width - PURCHASE_MENU_BPR*_button_width) / (PURCHASE_MENU_BPR + 1);

//Array that contains all of the button data.
buttons = [];


var _button_x = 0;//(window_get_width() - menu_width) + x_gap;
var _button_y = 0;//object_get_sprite(unit_purchase_button).width;
//Create buttons
for(var i = 0; i < array_length(button_purchase_data); i++) {
	if(i % PURCHASE_MENU_BPR == 0) { //Start of a new row
		_button_x = (window_get_width() - menu_width) + x_gap;
		_button_y += _button_width;
	}
	
	buttons.push(new UnitPurchaseButton(_button_x, _button_y, button_purchase_data[i]));
	_button_x += (_button_width + x_gap);
}*/