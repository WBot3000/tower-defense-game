/// @description Draw the menu background
/*
menu_width = window_get_width()/3;
menu_height = window_get_height();

//Currently set to being a third of the screen
//TODO: Might want to put these in a seperate file. These should really only need to be recalculated when the screen changes size.
if(show_menu) {
	draw_rectangle_color((2/3)*_ww, 0, _ww, _wh, c_silver, c_silver, c_silver, c_silver, false);
	for(var i = 0; i < array_length(buttons); ++i) {
		buttons[i].draw(window_get_width() - menu_width, 0);
	}
}
*/